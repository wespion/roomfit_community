name: roomfit_community_test1
description: RoomFit 커뮤니티 플랫폼

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Supabase
  supabase_flutter: ^2.5.6

  # 상태관리
  provider: ^6.1.2

  # HTTP 통신
  http: ^1.2.1

  # UI/UX
  google_fonts: ^6.2.1
  flutter_staggered_grid_view: ^0.7.0
  cached_network_image: ^3.3.1

  # 유틸리티
  timeago: ^3.6.1
  url_launcher: ^6.2.6
  image_picker: ^1.0.8

  # 라우팅
  go_router: ^14.2.0

  # 아이콘
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # 에셋 폴더 (나중에 추가)
  # assets:
  #   - assets/images/
  #   - assets/icons/